//Find grades of according to percentage.
#include<stdio.h>
#include<stdlib.h>

int main()
{
printf("Enter the percentage  marks");
scanf("%f",&per);
if(per>=90%)
   printf('Grade is A') ;
else if(per>=80%)
    printf('Grade is B');
else if (per=70%)
    printf('Grade is C');
else if (per>=60%)
    printf('Grade is D');
else if(per>=40%)
    printf('Grade is E' );
else if ("per<40%")
    printf('Grade is F');
    
    return 0;
    
}
